# ml-localization-failure-prediction
Comparison of Machine Learning methods (Random Forest, SVM, MLP, XGBoost) for predicting localization failures in autonomous mobile robots using LiDAR and AMCL particle filter data.
